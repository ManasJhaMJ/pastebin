// src/components/Stats.js
// Lifetime usage stats (all-time pastes created & viewed).
//
// Both stats are intentionally hidden from the frontend for now, so this
// renders nothing. The underlying counters in Firebase are still tracked
// (see ../stats.js). To restore the UI, fetch getStats() and render the
// desired stat block(s) here again.
function Stats() {
    return null;
}

export default Stats;
